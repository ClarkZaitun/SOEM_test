# EC_SYNC PI控制器算法说明

## 1. 算法概述

`ec_sync` 函数实现了一个 **PI控制器（比例-积分控制器）**，用于将Linux系统时间与EtherCAT从站的DC（Distributed Clock，分布式时钟）时间进行同步。

这是 EtherCAT 实时通信中的关键技术，通过PI控制器的闭环调节，使主站（Linux系统）能够精确跟踪从站的参考时钟。

## 2. 输入输出参数

### 输入参数

| 参数 | 类型 | 含义 | 单位 |
|------|------|------|------|
| `reftime` | `int64` | 从站参考时钟时间（DC时间） | 纳秒(ns) |
| `cycletime` | `int64` | 循环周期时间（通信周期） | 纳秒(ns) |

### 输出参数

| 参数 | 类型 | 含义 | 单位 |
|------|------|------|------|
| `offsettime` | `int64*` | 计算得到的时间偏移量，用于调整帧发送时间 | 纳秒(ns) |

## 3. 核心概念：为什么要提前50µs发送？

### 3.1 分布式时钟（DC）工作原理

EtherCAT从站内置精确的时钟（DC），从站在精确的时钟同步下工作。每个从站都有一个**DC同步点**，这是从站**预期**收到主站帧的时间。

### 3.2 Linux系统延迟问题

主站运行在Linux系统上，存在多种延迟：

| 延迟类型 | 原因 |
|---------|------|
| 调度延迟 | 线程切换、CPU竞争 |
| 网卡驱动延迟 | 驱动处理、发送队列 |
| 中断延迟 | 网卡中断处理 |
| 软件延迟 | 应用层到内核层的传递 |

这些延迟总和可能达到几十微秒，且不确定。

### 3.3 50µs 的作用

**50µs** 是一个经验值/补偿值，表示主站需要**比DC同步点提前50µs**发送帧：

```
帧发送时间 = DC同步时间 - 50µs - toff（PI调节量）
```

这样，帧经过网络传输延迟后，恰好在DC同步点到达从站。

## 4. 算法详解

### 4.1 第一步：计算相位差

```c
delta = (reftime - 50000) % cycletime;
```

- `reftime`：从站DC时间的当前值
- `- 50000`：减去50µs的补偿偏移
- `% cycletime`：取余得到在一个周期内的相位位置

**作用**：将绝对时间转换为相对于周期起点的相位，范围 `[0, cycletime)`

### 4.2 第二步：相位调整

```c
if(delta > cycletime / 2) { delta = delta - cycletime; }
```

- **理论依据**：将 `delta` 调整到 `[-cycletime/2, cycletime/2]` 范围内
- **目的**：确保控制器对正负偏差一视同仁，实现最小相位差调节
- 例如：如果 cycletime = 1000000ns (1ms)，则调整后 delta ∈ [-500000, 500000] ns

### 4.3 第三步：积分累积

```c
if(delta > 0) { integral++; }
if(delta < 0) { integral--; }
```

- **积分项作用**：消除稳态误差（static error）
- 当偏差持续存在时，积分项会不断累积，直到偏差被消除
- 这实现了无静差控制（zero steady-state error）

### 4.4 第四步：计算控制输出

```c
*offsettime = -(delta / 100) - (integral / 20);
```

- **比例项**：`-(delta / 100)`，增益系数 = 1/100 = 0.01
- **积分项**：`(integral / 20)`，增益系数 = 1/20 = 0.05
- **负号**：因为需要将Linux时间向DC时间靠近，所以取反

## 5. 调节目标分析

### 5.1 delta = 0 是目标吗？

**不完全是。** 更准确地说：

- **最终目标**：帧的发送时间 = DC时间 - 50µs
- **表现形式**：delta 在 0 附近波动

因为 DC时间（reftime）在不断变化，delta 不可能恒等于0，而是在 0 附近振荡。PI控制器的作用就是让这个振荡最小化。

### 5.2 调节过程

| 情况 | delta | toff 变化 | 帧发送时间 |
|------|-------|----------|-----------|
| 帧发送太早 | > 0 | toff 增大 | 晚下次发 |
| 帧发送太晚 | < 0 | toff 减小 | 早下次发 |
| 帧发送准时 | ≈ 0 | toff 稳定 | 保持 |

## 6. 算法如何影响帧发送时间

### 6.1 调用流程

在测试代码（如ebox.c）中：

```c
// 主循环
while(1)
{
    // 计算下次周期开始时间 = 当前时间 + 周期 + 偏移量
    add_timespec(&ts, cycletime + toff);  // ← toff 在这里起作用！
    
    // 休眠到指定时间
    pthread_cond_timedwait(&cond, &mutex, &ts);
    
    // 发送/接收过程数据
    ec_send_processdata();
    ec_receive_processdata(EC_TIMEOUTRET);
    
    // 计算新的偏移量
    ec_sync(ec_DCtime, cycletime, &toff);
}
```

### 6.2 toff 对休眠时间的影响

```
┌─────────────────────────────────────────────────────────────┐
│                      时间轴                                  │
│  ────────────cycletime─────────────────                     │
│                                                             │
│  帧发送时刻 ─────────┬───────────────────▶                 │
│                      │                                         │
│              ┌───────┴───────┐                               │
│              │  + toff       │  ← PI控制器调整这个值         │
│              └───────────────┘                               │
└─────────────────────────────────────────────────────────────┘

toff > 0：休眠时间变长 → 帧晚发
toff < 0：休眠时间变短 → 帧早发
```

### 6.3 闭环调节示意

```
DC时间  ──────────────────────────────▶
          ↓ 计算 delta
帧发送   ──┬─────────────────────────▶
  时间      │ 调整 toff
            ↓
    ┌───────────────────────────────────┐
    │  帧发送时间 = DC时间 - 50µs - toff │
    └───────────────────────────────────┘
            ↑
      循环调节
```

## 7. PI控制器理论

### 7.1 控制律

```
u(t) = Kp * e(t) + Ki * ∫e(t)dt
```

其中：
- `u(t)` = 控制输出（offsettime）
- `e(t)` = 误差信号（delta）
- `Kp` = 比例增益 = 0.01
- `Ki` = 积分增益 = 0.05

### 7.2 各部分作用

| 分量 | 作用 |
|------|------|
| **比例项 (P)** | 对当前偏差作出即时响应，提供快速调节 |
| **积分项 (I)** | 累积历史偏差，消除稳态误差，确保长期精度 |

### 7.3 增益选择依据

- **比例增益 1/100**：对偏差进行100:1的缩小，提供平滑的调节
- **积分增益 1/20**：对累积误差进行20:1的缩小，积分作用较强以确保快速消除静差
- 积分增益大于比例增益，使得积分项在稳态调节中起主导作用

## 8. 同步流程图

```
┌─────────────────────────────────────────────────────────────┐
│                     主循环                                   │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  1. 读取DC时间 (reftime)                                     │
│     - 从站返回的分布式时钟时间                               │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  2. 计算相位差                                               │
│     delta = (reftime - 50000) % cycletime                    │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  3. 相位调整                                                 │
│     delta ∈ [-cycletime/2, cycletime/2]                     │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  4. 积分累积                                                 │
│     integral++ (delta>0) 或 integral-- (delta<0)            │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  5. PI计算输出                                               │
│     toff = -(delta/100) - (integral/20)                      │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  6. 调整休眠时间                                             │
│     下次唤醒时间 += toff                                    │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  7. 帧在调整后的时间发送                                     │
│     最终效果：帧发送时间 ≈ DC时间 - 50µs                     │
└─────────────────────────────────────────────────────────────┘
```

## 9. 注意事项

1. **50µs延迟**：这是经验值，用于补偿Linux系统的不确定性延迟
   - 如果网络延迟较大，可能需要增大此值
   - 如果系统延迟较小，可以减小此值

2. **integral变量**：必须为全局或静态变量，在多次调用间保持累积状态

3. **单位一致性**：所有时间参数均为纳秒，确保运算精度

4. **饱和处理**：实际应用中可能需要添加积分饱和限制，防止积分项过大

5. **cycletime选择**：通常为 1ms（1000000ns）或 500µs（500000ns）

## 10. 参考资料

- EtherCAT Technology Group: EtherCAT Slave Information
- Classic Control Theory: PI Controller Design
- IEEE 1588: Precision Clock Synchronization Protocol
